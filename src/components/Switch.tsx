import React from 'react';

type SwitchProps = {
    children: React.ReactNode
    
};

export default function Switch(props: SwitchProps) {
    console.log(props)
    
    return(
        <div>
        </div>
    );
};