

import React from 'react';

type ComboBoxProps = {
    children: React.ReactNode
    
};

export default function ComboBox(props: ComboBoxProps) {
    console.log(props)
    
    return(
        <div>
        </div>
    );
};