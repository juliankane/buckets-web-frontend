import React from 'react';

type CheckboxProps = {
    children: React.ReactNode
    
};

export default function Checkbox(props: CheckboxProps) {
    console.log(props)
    
    return(
        <div>
        </div>
    );
};