import React from 'react';

type ButtonProps = {
    



    children: React.ReactNode
    
};

export default function Button(props: ButtonProps) {
    console.log(props)
    
    return(
        <div>
        </div>
    );
};