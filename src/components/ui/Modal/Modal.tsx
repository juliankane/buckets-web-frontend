import React from 'react';

type ModalProps = {
    children: React.ReactNode
    
};

export default function Modal(props: ModalProps) {
    console.log(props)
    
    return(
        <div>
        </div>
    );
};